# MS Griffe Backend Skill

Skill local para orientar implementações do backend da MS Griffe. Use `$msgriffe-backend` em tarefas Fastify, Prisma, PostgreSQL, autenticação, catálogo, pedidos, pagamentos, integrações e operação.

O ponto de entrada é [`SKILL.md`](SKILL.md). As referências complementam o documento sem substituir as fontes de verdade em `docs/` e o código existente.
